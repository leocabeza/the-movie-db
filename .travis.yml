language: node_js
cache:
  directories:
    - node_modules
node_js:
  - "10"
before_install:
  - echo "//registry.npmjs.org/:_authToken=\${NPM_AUTH_TOKEN}" > .npmrc
script:
  - npm run build
deploy:
  - provider: script
    script:
      - npx semantic-release@17
    on:
      branch: master
